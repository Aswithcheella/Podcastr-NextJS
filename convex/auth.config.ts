// https://smart-calf-95.clerk.accounts.dev
const authConfig = {
  providers: [
    {
      domain: "https://smart-calf-95.clerk.accounts.dev",
      applicationID: "convex",
    },
  ],
};

export default authConfig;
